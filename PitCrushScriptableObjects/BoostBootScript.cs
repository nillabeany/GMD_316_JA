using UnityEngine;

public class BoostBootScript : MonoBehaviour
{
    public ItemAbilityPickups BoosterBoots;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

}
